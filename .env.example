# Bloom Identity Card - Environment Variables

# JWT secret for signing agent auth tokens
JWT_SECRET=your_jwt_secret_here

# Dashboard URL for agent access
# For local development: http://localhost:3000
# For staging/preflight: https://preflight.bloomprotocol.ai
# For production: https://bloomprotocol.ai
DASHBOARD_URL=https://preflight.bloomprotocol.ai

# Network for agent wallet (base-mainnet or base-sepolia)
# base-mainnet = production (real USDC, real gas fees)
# base-sepolia = testnet (free tokens, for testing)
NETWORK=base-mainnet

# Coinbase CDP API Keys (optional - for real wallet creation)
# Get these from: https://portal.cdp.coinbase.com/projects
# CDP_API_KEY_ID=your_cdp_api_key_id
# CDP_API_KEY_SECRET=your_cdp_api_key_secret
# CDP_WALLET_SECRET=your_cdp_wallet_secret

# Redis (optional - for nonce tracking)
# REDIS_URL=redis://localhost:6379
